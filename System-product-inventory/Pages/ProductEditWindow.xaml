<Window x:Class="System_product_inventory.Pages.ProductEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:System_product_inventory.Pages"
        mc:Ignorable="d"
        Title="ProductEditWindow" Height="310" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,10">
            <TextBlock Text="Название:"/>
            <TextBox x:Name="NameBox" Height="25"/>
        </StackPanel>

        <StackPanel Margin="0,0,0,10" Grid.Row="1">
            <TextBlock Text="Категория:" FontWeight="Bold"/>
            <ComboBox x:Name="CategoryComboBox"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      Height="25"/>
        </StackPanel>

        <StackPanel Margin="0,0,0,10" Grid.Row="2">
            <TextBlock Text="Количество:"/>
            <TextBox x:Name="QuantityBox" Height="25"/>
        </StackPanel>

        <StackPanel Margin="0,0,0,10" Grid.Row="3">
            <TextBlock Text="Цена:"/>
            <TextBox x:Name="PriceBox" Height="25"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="4" Margin="0,10,0,0">
            <Button Content="Сохранить" Width="100" Margin="0,0,10,0" Click="Save_Click"/>
            <Button Content="Отмена" Width="100" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
