<Window x:Class="System_product_inventory.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:System_product_inventory"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель управления -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10" VerticalAlignment="Center" HorizontalAlignment="Stretch">
            <TextBox x:Name="SearchBox" Width="250" Height="30" Margin="0,0,10,0" VerticalContentAlignment="Center"/>

            <Button x:Name="SearchButton" Content="Поиск" Width="100" Height="30" Margin="0,0,20,0"/>

            <Button x:Name="AddButton" Content="Добавить" Width="100" Height="30" Margin="0,0,10,0" Background="#4CAF50" Foreground="White"/>
            <Button x:Name="EditButton" Content="Редактировать" Width="120" Height="30" Margin="0,0,10,0" Background="#2196F3" Foreground="White"/>
            <Button x:Name="DeleteButton" Content="Удалить" Width="100" Height="30" Background="#F44336" Foreground="White"/>
        </StackPanel>

        <!-- Таблица товаров -->
        <DataGrid x:Name="ProductsGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  AlternatingRowBackground="#f2f2f2"
                  RowHeight="35">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding Category}" Width="*"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="100"/>
                <DataGridTextColumn Header="Цена (₽)" Binding="{Binding Price}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Панель статистики -->
        <Border Grid.Row="2" Margin="0,10,0,0" Padding="10" Background="#eeeeee" CornerRadius="5">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" >
                <TextBlock Text="Общая сумма: " FontWeight="Bold"/>
                <TextBlock x:Name="TotalSumText" Text="0 ₽" Margin="5,0,20,0"/>
                <TextBlock Text="Средняя цена (по категории): " FontWeight="Bold"/>
                <TextBlock x:Name="AveragePriceText" Text="0 ₽" Margin="5,0,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
